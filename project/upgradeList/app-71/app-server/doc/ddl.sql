CREATE TABLE myapp_soldier (
    soldier_no INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    age INT,
    password VARCHAR(255),
    rank VARCHAR(255),
    enlis_date DATE,
    dis_date DATE,
    d_day BIGINT
);
